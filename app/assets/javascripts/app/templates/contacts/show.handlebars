{{#if view.isEditing}}
    <td colspan="5">
        {{view App.EditContactView}}
    </td>
{{else}}
    <td>{{first_name}}</td>
    <td>{{last_name}}</td>
    <td>{{email}}</td>
    <td><a href="#" {{action "edit"}}>Edit</a></td>
    <td><a href="#" {{action "delete"}}>Delete</a></td>
{{/if}}